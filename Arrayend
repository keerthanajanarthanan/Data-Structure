#include <stdio.h>

int main() {
    int num[6]; 
    int size = 5;   
    for (int i = 0; i < size; i++) {
        scanf("%d", &num[i]);
    }
    int NE;
    scanf("%d", &NE);
    num[size] = NE; 
    size++; 
    printf("DISPLAY");
    for (int i = 0; i < size; i++) {
        printf("%d ", num[i]);
    }

    return 0;
}
